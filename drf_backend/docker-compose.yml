version: "3.9"

services:
  #  db:
  #    image: postgres
  #     container_name: drf_vue_db
  #    environment:
  #      - POSTGRES_DB=
  #      - POSTGRES_USER=
  #      - POSTGRES_PASSWORD=
  #    ports:
  #      - "5432:5432"
  app:
    build: .
    container_name: drf_backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app/
    ports:
      - "8000:8000"
#    depends_on:
#      - db